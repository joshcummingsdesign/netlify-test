{
  "name": "netlify-test",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "PORT=8080 react-scripts start",
    "build": "react-scripts build",
    "check": "retire -n -p",
    "test": "react-scripts test",
    "cy:open": "cypress open",
    "cy:run": "cypress run",
    "e2e": "yarn build && start-server-and-test 'http-server build -c-1 --silent' 8080 cy:run",
    "predeploy": "yarn check && yarn test && yarn e2e"
  },
  "dependencies": {
    "react": "^16.8.6",
    "react-dom": "^16.8.6"
  },
  "devDependencies": {
    "cypress": "^3.3.1",
    "http-server": "^0.11.1",
    "react-scripts": "3.0.1",
    "retire": "^1.6.3",
    "start-server-and-test": "^1.9.1"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
