{
  "name": "netlify-test",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "PORT=8080 react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "cy:open": "cypress open",
    "cy:run": "cypress run",
    "start:ci": "http-server build -c-1 --silent",
    "e2e": "start-server-and-test start:ci 8080 cy:run",
    "predeploy": "CI=1 npm i cypress; npm run e2e"
  },
  "dependencies": {
    "react": "^16.8.6",
    "react-dom": "^16.8.6"
  },
  "devDependencies": {
    "cypress": "^3.3.1",
    "http-server": "^0.11.1",
    "react-scripts": "3.0.1",
    "start-server-and-test": "^1.9.1"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
